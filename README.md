# From-Scratch-Neural-Net-Training-Dashboard
build a CNN/MLP from scratch using raw PyTorch tensors, train it on MNIST with full GPU support, and watch it learn in real time via a React dashboard. The frontend lets you configure hyperparameters, pick a preset model, choose optimizer/loss/device — and an advanced tab lets you build a custom architecture layer by layer.
